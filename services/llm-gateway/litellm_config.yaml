# litellm_config.yaml — model routing for the LiteLLM proxy (llm-gateway).
# Author: ai-circus-framework contributors
#
# Add/remove model_list entries to change what's available through the gateway's
# OpenAI-compatible API — every consumer (assistant, rag-agent, ui-react/CopilotKit)
# calls a `model_name` from this list, never a provider SDK directly. Provider API
# keys are read from the real environment via the `os.environ/<NAME>` syntax — see
# settings.yaml for which vars to set in .env.

model_list:
  - model_name: gpt-4o-mini
    litellm_params:
      model: openai/gpt-4o-mini
      api_key: os.environ/OPENAI_API_KEY

  - model_name: gemini-flash
    litellm_params:
      model: gemini/gemini-2.0-flash
      api_key: os.environ/GOOGLE_API_KEY

  # Optional — unreachable (no default model selected) until an operator sets
  # AZURE_OPENAI_API_KEY/_BASE and edits `azure/<deployment>` below to match their
  # actual Azure deployment name (litellm's `os.environ/VAR` substitution only
  # replaces a value that's *entirely* that token, so the deployment name — which
  # varies per Azure resource — can't be env-substituted inside this prefixed
  # string). Also configurable at runtime, without a restart, via the ui-react
  # Settings page → platform-registry's /llm-settings/* (see
  # services/platform-registry/src/platform_registry/core/llm_settings.py), which
  # builds the full "azure/<deployment>" string itself — no YAML edit needed there.
  - model_name: azure-gpt4o
    litellm_params:
      model: azure/gpt-4o-mini # <- replace with your deployment name
      api_key: os.environ/AZURE_OPENAI_API_KEY
      api_base: os.environ/AZURE_OPENAI_API_BASE

  # Fully local/no-API-key option — platform-agnostic, no cloud dependency (carried
  # over from mlops_templates' original goals). Requires an Ollama instance the
  # operator already runs (host machine or their own container) with the model
  # pulled — llama3.2:3b is ~2GB, chosen so it fits in 3-4GB RAM; OLLAMA_API_BASE is
  # unset by default, so this entry is simply unreachable until configured (env var
  # or the Settings page, same as azure-gpt4o above).
  - model_name: llama3
    litellm_params:
      model: ollama/llama3.2:3b
      api_base: os.environ/OLLAMA_API_BASE

general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY
  # Persistent spend-tracking (general_settings.database_url) is deliberately NOT
  # enabled: it requires litellm's `prisma` Python client, which needs a Node.js-based
  # `prisma generate` codegen step at build time that this image doesn't run. The
  # proxy still routes models and enforces the master key without it — a documented
  # follow-up (see root README "Reserved for later") to add the full prisma toolchain
  # if persistent spend history becomes a requirement.
