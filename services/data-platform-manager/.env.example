# Active Environment Profile (local, docker, staging, production)
APP_ENVIRONMENT=local

# Postgres password
POSTGRES_PASSWORD=

# Bearer token required on every route (shared with other services' admin-key bypass)
ADMIN_API_KEY=

# API key presented to llm-gateway (its LITELLM_MASTER_KEY) for admin calls
LLM_GATEWAY_API_KEY=

# SeaweedFS secret key (must match OBJECT_STORE_SECRET_KEY in the repo root .env)
OBJECT_STORE_SECRET_KEY=
