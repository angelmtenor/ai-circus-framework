# Mirrors docker-compose.yml's rag-agent service.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rag-agent
  namespace: ai-circus
spec:
  replicas: 1
  selector:
    matchLabels: {app: rag-agent}
  template:
    metadata:
      labels: {app: rag-agent}
    spec:
      containers:
        - name: rag-agent
          image: ai-circus/rag-agent:local
          imagePullPolicy: Never
          ports:
            - {containerPort: 8000}
          envFrom:
            - configMapRef: {name: shared-config}
            - secretRef: {name: app-env}
          env:
            # Renamed from .env's LITELLM_MASTER_KEY.
            - {name: LLM_GATEWAY_API_KEY, valueFrom: {secretKeyRef: {name: app-env, key: LITELLM_MASTER_KEY}}}
          volumeMounts:
            - {name: scenarios, mountPath: /app/scenarios, readOnly: true}
          readinessProbe:
            httpGet: {path: /healthz, port: 8000}
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet: {path: /healthz, port: 8000}
            initialDelaySeconds: 15
            periodSeconds: 10
      volumes:
        - name: scenarios
          hostPath: {path: /scenarios, type: Directory}
---
apiVersion: v1
kind: Service
metadata:
  name: rag-agent
  namespace: ai-circus
spec:
  selector: {app: rag-agent}
  ports:
    - {port: 8000, targetPort: 8000}
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: rag-agent
  namespace: ai-circus
spec:
  entryPoints: [web]
  routes:
    - match: Host(`rag-agent.localhost`)
      kind: Rule
      services:
        - {name: rag-agent, port: 8000}
