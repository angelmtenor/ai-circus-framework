# Mirrors docker-compose.yml's assistant service.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: assistant
  namespace: ai-circus
spec:
  replicas: 1
  selector:
    matchLabels: {app: assistant}
  template:
    metadata:
      labels: {app: assistant}
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000  # matches the 'app' user every services/*/Dockerfile creates via useradd
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: assistant
          image: ai-circus/assistant:local
          imagePullPolicy: Never
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: {drop: ["ALL"]}
          ports:
            - {containerPort: 8000}
          envFrom:
            - configMapRef: {name: shared-config}
            - secretRef: {name: assistant-secrets}
          env:
            # Renamed from .env's LITELLM_MASTER_KEY.
            - {name: LLM_GATEWAY_API_KEY, valueFrom: {secretKeyRef: {name: assistant-secrets, key: LITELLM_MASTER_KEY}}}
          volumeMounts:
            - {name: scenarios, mountPath: /app/scenarios, readOnly: true}
          readinessProbe:
            httpGet: {path: /healthz, port: 8000}
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet: {path: /healthz, port: 8000}
            initialDelaySeconds: 15
            periodSeconds: 10
      volumes:
        - name: scenarios
          hostPath: {path: /scenarios, type: Directory}
---
apiVersion: v1
kind: Service
metadata:
  name: assistant
  namespace: ai-circus
spec:
  selector: {app: assistant}
  ports:
    - {port: 8000, targetPort: 8000}
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: assistant
  namespace: ai-circus
spec:
  entryPoints: [web]
  routes:
    - match: Host(`assistant.localhost`)
      kind: Rule
      services:
        - {name: assistant, port: 8000}
